<% provide :page_title, "Articles" %>

<div class="container">
    <h1><%= t('.title') %></h1>

    <ul>
      <% @articles.each do |article| %>
        <li>
          <%= link_to article.title, article %>
        </li>
        <p>
          <%= truncate(article.body, length: 250) { link_to t('.continued'), article }%>
        <p><%= t('.publish') %>:
          <%= article.formatted_created_at %>
        </p>
        </p><hr>
      <% end %>
    </ul>

    <%== pagy_bootstrap_nav(@pagy) %>
</div>
